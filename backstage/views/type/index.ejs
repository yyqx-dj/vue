<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>类别列表</title>
</head>

<body>
    <!--The content below is only a placeholder and can be replaced.-->

    <link rel="stylesheet" href="/bootstrap/css/bootstrap.css">


    <!-- top -->
    <%- include  ../common/top.ejs   %>


    <div class="container-fluid">
        <div class="row">


            <!-- left -->

            <%- include  ../common/left.ejs   %>

            <div class="col-sm-10">
                <ol class="breadcrumb">
                    <li class="active">类别管理
                    </li>
                    <li class="active">列表列表
                    </li>
                </ol>
                <div class="panel panel-default">
                    <div class="panel-heading">
                        搜索
                    </div>
                    <div class="panel-body">
                        <form role="form" class="form-inline">
                            <div class="form-group">
                                <label for="name">名称</label>
                                <input type="text" class="form-control" id="name" placeholder="请输入名称">
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn-default">开始搜索</button>
                            </div>
                        </form>
                    </div>
                </div>
                <!--
                列表展示
            -->
                <div class="table-responsive">
                    <table class="table table-striped ">
                        <thead>
                            <tr>
                                <th>编号</th>
                                <th>图标</th>
                                <th>名称</th>
                                <th class="text-center">操作</th>
                            </tr>
                        </thead>
                        <tbody>
                            <%  for(let i  in types) { %>
                            <tr>
                                <td><%= types[i]._id %></td>
                                <td>
                                    <img src="/<%= types[i].typepic %>" alt="暂无图片" width="30px" height="30px">
                                </td>
                                <td><%= types[i].typename %></td>
                                <td class="text-center">
                                    <a href="/type/edit?id=<%= types[i]._id %>" class="btn btn-info">修改</a>
                                    <a href="/type/del?id=<%= types[i]._id %>&typepic=<%= types[i].typepic %>" class="btn btn-warning">删除</a>
                                </td>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>

</body>

</html>