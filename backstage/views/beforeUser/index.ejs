<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>用户列表</title>
</head>

<body>
    <!--The content below is only a placeholder and can be replaced.-->

    <link rel="stylesheet" href="/bootstrap/css/bootstrap.css">


    <!-- top -->
    <%- include  ../common/top.ejs   %>


    <div class="container-fluid">
        <div class="row">


            <!-- left -->

            <%- include  ../common/left.ejs   %>

            <div class="col-sm-10">
                <ol class="breadcrumb">
                    <li class="active">购物车管理
                    </li>
                    <li class="active">购物车列表
                    </li>
                </ol>
                <div class="panel panel-default">
                    <div class="panel-heading">
                        搜索
                    </div>
                    <div class="panel-body">
                        <form role="form" class="form-inline">
                            <div class="form-group">
                                <label for="name">名称</label>
                                <input type="text" class="form-control" id="name" placeholder="请输入名称">
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn-default">开始搜索</button>
                            </div>
                        </form>
                    </div>
                </div>
                <!--
                列表展示
            -->
                <div class="table-responsive">
                    <table class="table table-striped ">
                        <thead>
                            <tr>
                                <th>编号</th>
                                <th>头像</th>
                                <th>昵称</th>
                                <th>手机号码</th>
                                <th>密码</th>
                                <th>身份证号</th>
                                <th>邮箱</th>
                                <th>性别</th>
                                <th class="text-center">操作</th>
                            </tr>
                        </thead>
                        <tbody>
                            <%  for(let i  in beforeUser) { %>
                            <tr>
                                <td><%= beforeUser[i]._id %></td>
                                <td>
                                    <img src="/<%= beforeUser[i].userpic %>" alt="暂无图片" width="30px" height="30px">
                                </td>
                                <td><%= beforeUser[i].nickname %></td>
                                <td><%= beforeUser[i].phone %></td>
                                <td><%= beforeUser[i].pass %></td>
                                <td><%= beforeUser[i].idx %></td>
                                <td><%= beforeUser[i].email %></td>
                              
                                <td><%= beforeUser[i].sex %></td>
                                <!-- <td>
                                    <% if(beforeUser[i].sex){ %>
                                    男
                                    <%}else{%>
                                    女
                                    <%}%>
                                    
                                </td> -->
                                <td class="text-center">
                                   xxx
                                </td>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>

</body>

</html>