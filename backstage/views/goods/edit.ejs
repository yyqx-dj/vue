<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>商品修改</title>
</head>

<body>
    <!--The content below is only a placeholder and can be replaced.-->

    <link rel="stylesheet" href="/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" href="/css/basic.css">


    <!-- top -->
    <%- include  ../common/top.ejs   %>
    <div class="container-fluid">
        <div class="row">
            <!-- left -->
            <%- include  ../common/left.ejs   %>
            <div class="col-sm-10">
                <ol class="breadcrumb">
                    <li class="active">商品管理
                    </li>
                    <li class="active">商品列表
                    </li>
                </ol>
                <div class="panel panel-default">

                    <div class="panel-heading">
                        修改商品
                    </div>
                    <div class="panel-body">
                        <div class="table-responsive input-form">
                            <form action="/goods/doEdit" method="post" enctype="multipart/form-data">
                                <ul>
                                    <li> 商品名称: <input type="text" name="goodsname" value="<%= good.goodsname %>" /></li>
                                    <li> 商品图片: <input type="file" name="goodspic" /></li>
                                    <!-- 传递id，作为修改的条件 -->
                                    <input type="hidden" name="id" value="<%= good._id %>">
                                    <!-- 传递原来老的图片名，用来删除原来图片的 -->
                                    <input type="hidden" name="oldpic" value="<%= good.goodspic %>">

                                    <li> 商品价格: <input type="text" name="price" value="<%= good.price %>" /></li>
                                    <li> 商品类别:
                                        <select name="typeid" id="">
                                            <!-- 
                                                当前商品类别的id（typeid） 等于 当前的类别id时，加上selected
                                             -->
                                            <% for(let i  in types){  %>
                                            <option value="<%=  types[i]._id %>" <%= types[i]._id == good.typeid ? 'selected' : "" %> ><%=  types[i].typename %></option>
                                            <%}%>
                                    </select>
                                    </li>
                                    <li> 商品库存: <input type="text" name="num" value="<%= good.num %>" /></li>
                                    <li> 商品描述: <input type="text" name="desc" value="<%= good.desc %>" /></li>
                                    <li> 商品状态:
                                        <select name="status" id="">
                                            <option value="1" <%= good.status == 1 ? 'selected' : "" %>>新品</option>
                                            <option value="2" <%= good.status == 2 ? 'selected' : "" %>>热卖</option>
                                            <option value="3" <%= good.status == 3 ? 'selected' : "" %>>下架</option>
                                        </select>
                                    </li>
                                    <li>
                                        <br />
                                        <button type="submit" class="btn btn-default">修改</button>
                                    </li>
                                </ul>
                            </form>
                        </div>
                    </div>


                </div>

            </div>
        </div>
    </div>

</body>

</html>